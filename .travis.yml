language: true

before_install:
  - chmod +x ./build/testCSV.sh
  - chmod +x .api

install: true

script:
  - ./build/testCSV.sh dataset.csv 4 username+matricula.csv 2 > err
  
after_success:
  - ./.api true
  
after_failure:
  - ./.api false
  
